import React from "react";
import styled from "styled-components";

const StyledVideoPlayer = styled.div``;

const VideoPlayer = props => {
  return <StyledVideoPlayer></StyledVideoPlayer>;
};

export default VideoPlayer;
