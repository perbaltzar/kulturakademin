import React from "react";
import styled from "styled-components";

const StyledCookies = styled.div``;

const Cookies = props => {
  return <StyledCookies></StyledCookies>;
};

export default Cookies;
